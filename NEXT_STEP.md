# Next Step

## P0 - 当前执行指针

1. 发布 `@cory-ronghua/video-fetch-mcp@1.0.1` 后，执行 `claude mcp remove video-fetch-mcp -s local && claude mcp add video-fetch-mcp -- npx -y @cory-ronghua/video-fetch-mcp`，验证 MCP 状态为 `✓ Connected`
